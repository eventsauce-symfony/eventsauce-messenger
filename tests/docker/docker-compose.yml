version: '3'

services:
    php:
        build:
            context: ./php8
        container_name: eventsauce_messenger_php
        volumes:
            - ../../:/var/library:rw
        networks:
            - default
networks:
    default:
